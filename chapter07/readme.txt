1、模板方法模式，定义一个操作中算法的骨架，而将一些步骤延迟到子类中。模板方法使得子类可以不改变一个算法的结构
即可重新定义该算法的某些特定步骤。模板方法模式一般包含两个类，分别是AbstractClass类和ConcreteClass类，其中
AbstractClass类实现了一个模板方法，定义了算法的骨架，而逻辑的组成步骤在相应的抽象操作中，推迟到子类实现；
ConcreteClass类实现了父类中的抽象操作，使得不同的子类对于父类中的顶级逻辑的实现各不相同。
2、模板方法模式是通过把不变行为搬移到父类当中，去除子类中的重复代码来体现它的优势。